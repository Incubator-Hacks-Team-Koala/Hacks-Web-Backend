# Hacks-Web-Backend
Hack Web Python - Django project

## Setup

`pip install -r requirements.txt`

### ./my.cnf
```
[client]
host = HOST
database = NAME
user = USER
password = PASSWORD
default-character-set = utf8
```